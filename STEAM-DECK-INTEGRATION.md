# Steam Deck Manager - Retrowave Integration

Интеграция retrowave темы в приложение управления Steam Deck.

## 🎮 Обзор

Steam Deck Manager теперь имеет красивый веб-интерфейс в стиле retrowave/synthwave, который позволяет управлять всеми скриптами и настройками Steam Deck через браузер.

## 📁 Файлы интеграции

- `steam-deck-manager.html` - Основной веб-интерфейс
- `retrowave-theme.css` - Стили retrowave темы
- `retrowave-effects.js` - JavaScript эффекты
- `start-steam-deck-manager.sh` - Скрипт запуска
- `STEAM-DECK-INTEGRATION.md` - Документация интеграции

## 🚀 Быстрый запуск

1. Запустите веб-интерфейс:
```bash
./start-steam-deck-manager.sh
```

2. Откройте браузер и перейдите по адресу:
```
http://localhost:8000/steam-deck-manager.html
```

## 🎯 Функциональность

### Управление скриптами

- **SSH Управление** - включение/отключение SSH
- **Управление играми** - сохранение и восстановление игр
- **Управление библиотеками** - добавление внешних библиотек Steam
- **GPU Управление** - настройка внешней GPU
- **Переключение режимов** - игровой режим / рабочий стол

### Мониторинг системы

- Загрузка CPU в реальном времени
- Использование RAM
- Использование диска
- Температура системы

### Логи и отладка

- Системные логи в реальном времени
- Экспорт логов
- Цветовая индикация статусов

## 🎨 Retrowave особенности

### Визуальные эффекты

- Неоновая цветовая палитра
- Анимированный фон с сеткой
- Пульсирующие элементы
- Свечение при наведении
- Плавные анимации переходов

### Звуковые эффекты

- Синтезаторные звуки при взаимодействии
- Звуки при наведении на кнопки
- Звуки при выполнении операций

### Интерактивность

- Эффект печатания для заголовков
- Глитч-эффекты при наведении
- Система частиц на фоне
- Адаптивный дизайн

## 🔧 Настройка

### Изменение цветов

Отредактируйте CSS переменные в `retrowave-theme.css`:

```css
:root {
  --neon-pink: #ff0080;      /* Ваш цвет */
  --neon-cyan: #00ffff;      /* Ваш цвет */
  --neon-purple: #8000ff;    /* Ваш цвет */
  /* ... */
}
```

### Отключение эффектов

В настройках интерфейса можно отключить:
- Звуковые эффекты
- Анимации
- Автообновление статуса

### Настройка обновления

Измените интервал обновления системной информации:
- Минимум: 1 секунда
- Максимум: 60 секунд
- По умолчанию: 5 секунд

## 📱 Адаптивность

Интерфейс автоматически адаптируется под разные размеры экранов:

- **Desktop** (> 768px) - Полные эффекты и функциональность
- **Tablet** (768px) - Упрощенные эффекты
- **Mobile** (< 768px) - Оптимизированный для мобильных

## 🔌 API интеграция

### Реальные скрипты

Для подключения к реальным скриптам Steam Deck замените симуляцию в JavaScript на реальные вызовы:

```javascript
// Вместо симуляции
enableSSH() {
    this.log('Включение SSH...', 'info');
    // Реальный вызов скрипта
    fetch('/api/ssh/enable', { method: 'POST' })
        .then(response => response.json())
        .then(data => {
            this.log('SSH успешно включен', 'success');
        })
        .catch(error => {
            this.log('Ошибка включения SSH', 'error');
        });
}
```

### Системная информация

Для получения реальных данных системы:

```javascript
updateSystemInfo() {
    fetch('/api/system/info')
        .then(response => response.json())
        .then(data => {
            document.getElementById('cpu-usage').textContent = data.cpu + '%';
            document.getElementById('memory-usage').textContent = data.memory + '%';
            // ...
        });
}
```

## 🛠️ Разработка

### Структура проекта

```
steam-deck-manager/
├── steam-deck-manager.html    # Основной интерфейс
├── retrowave-theme.css        # Стили темы
├── retrowave-effects.js       # JavaScript эффекты
├── start-steam-deck-manager.sh # Скрипт запуска
└── STEAM-DECK-INTEGRATION.md  # Документация
```

### Добавление новых функций

1. Добавьте HTML в соответствующий раздел
2. Создайте JavaScript функцию
3. Добавьте стили при необходимости
4. Обновите документацию

### Тестирование

1. Запустите локальный сервер
2. Откройте интерфейс в браузере
3. Протестируйте все функции
4. Проверьте адаптивность

## 🐛 Устранение неполадок

### Веб-сервер не запускается

```bash
# Проверьте, занят ли порт 8000
netstat -tulpn | grep :8000

# Используйте другой порт
python3 -m http.server 8080
```

### Звуковые эффекты не работают

- Убедитесь, что браузер поддерживает Web Audio API
- Проверьте, что пользователь взаимодействовал со страницей
- Включите звуковые эффекты в настройках

### Анимации тормозят

- Отключите анимации в настройках
- Уменьшите количество частиц в `retrowave-effects.js`
- Используйте `will-change` CSS свойство

## 📄 Лицензия

Интеграция распространяется под той же лицензией, что и основное приложение.

## 🤝 Вклад в проект

1. Создайте форк проекта
2. Внесите изменения
3. Протестируйте функциональность
4. Создайте pull request

## 📞 Поддержка

Если у вас есть вопросы или проблемы:

1. Проверьте документацию
2. Создайте issue в репозитории
3. Обратитесь к сообществу

---

**Создано с ❤️ в стиле 80-х для Steam Deck**

*Управляйте своим Steam Deck в стиле retrowave!*
